@component('mail::message')
# File access request expired

Your request for **{{ $researchTitle }}** ({{ $fileType }}) expired without approval.

You may submit a new request if access is still needed.
@endcomponent